{
  "private": true,
  "scripts": {
    "prepare": "yarn tslint && lerna run prepare",
    "rebuild:browser": "theia rebuild:browser",
    "rebuild:electron": "theia rebuild:electron",
    "tslint-fix": "tslint --fix --project emacs-extension",
    "tslint": "tslint --project emacs-extension"
  },
  "devDependencies": {
    "lerna": "2.4.0",
    "tslint": "5.10.0"
  },
  "workspaces": [
    "emacs-extension", "browser-app", "electron-app"
  ]
}
